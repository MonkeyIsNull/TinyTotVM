PUSH_INT 0          ; change to 1 or 0 to test
JZ else_block       ; if top == 0, go to else

PUSH_STR "Condition is true"
PRINT
JMP end_if

LABEL else_block
PUSH_STR "Condition is false"
PRINT

LABEL end_if
HALT

